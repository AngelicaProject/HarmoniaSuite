Translate this JSON array of objects {i, s} with Final Fantasy XIV game strings from English to Russian. Copy all <tags> and placeholders from s into the translation exactly (same lowercase, never translated); keep their order unless Russian grammar requires otherwise. You may only add tags the game client understands (such as gender branches) when Russian grammar needs them, never invent other markup. Keep line breaks as-is. Text like \<word> is escaped literal text, keep the backslash. Return ONLY a JSON array of objects {i, t} with the translated strings, same i values, no explanations.
